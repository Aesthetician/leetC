/*

idea1:
two pointer 
[      ]
  L
     R
     swap(s[L], s[R])
while (left <= right): 
    if (s[left] is not voel) left++;
    if (s[right] is not voel) right--;
    if (s[left] and s[right] are vowel): 
        swap(s[left, s[right])
        left++;
        right--;
*/
